import React, { useState } from "react"; export default function ProjectForm({ onSave, onCancel }) { const [form, setForm] = useState({ name: "", client: "", site: "", area: "", material: "", qcTech: "" }); const update = (k, v) => setForm(c => ({ ...c, [k]: v })); const submit = (e) => { e.preventDefault(); if (form.name.trim()) onSave({ ...form, name: form.name.trim() }); }; return ( <div className="project-home" data-linersync-mounted="true"> <section className="card project-form-card"> <p className="eyebrow">NEW PROJECT</p> <h1>Create Job</h1> <form className="project-form" onSubmit={submit}> {["name", "client", "site", "area", "material", "qcTech"].map(f => ( <label key={f}> {f.charAt(0).toUpperCase() + f.slice(1)} {f === "name" && "*"} <input value={form[f]} onChange={e => update(f, e.target.value)} autoFocus={f === "name"} /> </label> ))} <div className="button-row"> <button type="button" onClick={onCancel}>Cancel</button> <button className="green" type="submit" disabled={!form.name.trim()}>Create Project</button> </div> </form> </section> </div> ); }
