import React from "react";
export default function Logs({ logs, onEdit, onCopy, onDelete, onLock }) { return <section className="page"><h3>Logs</h3>{logs.map((l)=><article key={l.id} className="log"><strong>{l.title}</strong><p>{l.status} • {l.type}</p><p>{l.capturedAt}</p><p>{l.gps?.lat ? `${l.gps.lat}, ${l.gps.lng}` : "No GPS"}</p><p>Warnings: {(l.mythosAudit?.warnings||[]).join("; ")||"None"}</p><div className="button-row"><button onClick={()=>onEdit(l)}>Edit</button><button onClick={()=>onCopy(l)}>Copy</button><button onClick={()=>onDelete(l.id)}>Delete</button><button onClick={()=>onLock(l)}>Lock</button></div></article>)}</section>; }
